package com.malevolentgods.fourtwenty.events;

// Temporarily disabling event handler to avoid conflicts with custom tab
public class ModEvents {
    // Custom creative tab handles item registration through displayItems callback
    // No event handlers needed for now
}
